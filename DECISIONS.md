# Decisions log

Every tuneable constant and visual-quality decision lands here with its reason.

| Date | Decision | Value | Why |
|---|---|---|---|
| 2026-09-14 | Stack | Native Swift + SwiftUI, no SPM deps | Master prompt §2: ScreenCaptureKit + Metal + VideoToolbox quality, headless xcodebuild |
| 2026-09-14 | Deployment target | macOS 14.0 | Master prompt §0/§2 |
| 2026-09-14 | Signing | Ad-hoc (`CODE_SIGN_IDENTITY = "-"`), no sandbox | Local-only distribution, zero cost, zero accounts; TCC re-prompt per rebuild is documented in README |
| 2026-09-14 | Project format | objectVersion 77, file-system-synchronized groups | Agents add Swift files without touching project.pbxproj |
| 2026-09-15 | Global hotkey | ⌘⇧2 via Carbon `RegisterEventHotKey` (fixed default, shown read-only in settings) | Works without Input Monitoring, frontmost-app independent; configurable hotkey deferred |
| 2026-09-15 | Default capture settings | 60 fps, source-native resolution, system audio ON, countdown OFF, hide-desktop-icons OFF | Screen Studio parity: 60 fps is the quality baseline; caps (1080p/4K by height) only shrink output |
| 2026-09-15 | Resolution cap semantics | Cap by HEIGHT (1080 / 2160), aspect preserved, rounded to even pixels | H.264 needs even dims; height-cap matches "1080p"/"4K" naming |
| 2026-09-15 | Writer threading | One serial DispatchQueue per writer input (screen video / screen audio / webcam); frames dropped when `isReadyForMoreMediaData` is false | Never block the capture pipeline; AVAssetWriterInput is not thread-safe for concurrent appends |
| 2026-09-15 | Pause math | Shared `PauseCompensator` (host-clock seconds domain, one instance per recording) subtracts accumulated paused time from buffer PTS; buffers dropped while paused | Single clock keeps screen video, system audio, webcam, mic and events.json aligned across pauses; mic-mute and camera-off use per-track compensators in WebcamRecorder |
| 2026-09-15 | Event tap | `CGEvent.tapCreate` .cghidEventTap listen-only on a dedicated Thread's CFRunLoop; timestamps from CGEvent.timestamp (mach) converted via mach_timebase_info | Tap callbacks must live on a run loop; mach-domain matches buffer PTS so event `t` aligns with video |
| 2026-09-15 | cursorType events | Schema supports `cursorType(name)`; runtime logs a single `arrow` at t=0 | Reliable cross-app cursor-type detection needs polling/AX tricks — deferred, flagged in roadmap risk list |
| 2026-09-15 | Area-mode sourceRect | `SCStreamConfiguration.sourceRect` set in PIXELS (points × backingScaleFactor, top-left origin) per Phase 1 brief | **Live-verify item**: Apple docs describe sourceRect as points in some contexts; if the recorded region is wrong, divide by scale in `buildCapturePlan()` (.area case) — single-line fix |
| 2026-09-15 | Pill panel | NSPanel .borderless + .nonactivatingPanel, level .statusBar, canJoinAllSpaces + fullScreenAuxiliary; pickers/countdown use .screenSaver level | Non-activating keeps focus on the user's apps; picker overlays need key status (Esc) so they activate the app transiently |
| 2026-09-15 | UI tick rates | Elapsed timer 0.1 s; mic level meter 10 Hz (−50…0 dB → 0…1); countdown 1 s steps | Cheap, smooth enough for a pill |
| 2026-09-15 | Output layout | `~/Movies/VibeStudio/<yyyy-MM-dd_HH-mm-ss>/{recording.mov, webcam.mov?, events.json, recording-meta.json}` | Per-session folder; meta carries capture mapping + first-buffer host times (sync offsets) for Phase 2/3 alignment |
| 2026-09-15 | Self-exclusion from capture | SCContentFilter(display:excludingWindows:) with all SCWindows owned by our bundle ID (pill, overlays, countdown) | Window-mode filter is desktopIndependentWindow so the pill is excluded by construction |
| 2026-09-15 | Sync offset | recording-meta.json stores screen/webcam/mic first-buffer host-clock seconds | Both SCKit and AVCaptureSession PTS are host-clock based, so simple subtraction aligns files in Phase 2 |
| 2026-09-15 | Cursor smoothing (§3.2) | Resample to 1/frameRate grid; jitter threshold 2 px measured from last ACCEPTED anchor (slow drifts accumulate past it); critically-damped spring per axis, c = 2√k, semi-implicit Euler; presets Rapid k=500 / Quick k=250 / Default k=120 / Slow k=60 | Anchor-based jitter suppression kills dwell noise without freezing slow movement; critical damping = no overshoot; k range spans ~0.1 s to ~0.5 s settle time |
| 2026-09-15 | CGRect/CGSize Codable wire format | Arrays: CGRect → `[[x,y],[w,h]]`, CGSize → `[w,h]` (CoreGraphics conformance) | events.json/meta JSON must match byte-for-byte; the Python fixture generator emits this shape |
| 2026-09-15 | Dev fixture | `scripts/make_fixture.sh` (ffmpeg testsrc2 1920x1080@60 + python3 events/meta generator) → `Fixtures/sample-recording/`, gitignored | Real recording is permission-blocked; deterministic fixture unblocks Phase 2 and schema-compatibility tests (tests XCTSkip if absent) |
| 2026-09-15 | .vibestudio bundle | Package directory: recording.mov, webcam.mov?, events.json, recording-meta.json, project.json (ProjectState v1: file refs, createdAt, appVersion) | Matches master prompt §2; import copies a loose Phase-1 folder to `<name>.vibestudio` beside it |
| 2026-09-15 | Editor window | AppKit NSWindow + NSHostingView (SwiftUI VideoPlayer/AVPlayer), opens on recording finish / openFile / `-open` launch arg; app stays .accessory, NSApp.activate() on open | Screen Studio-like: no Dock icon churn, editor appears above everything when summoned |
| 2026-09-15 | Cursor debug overlay | SwiftUI Canvas over the player; raw path red 1 pt @70%, smoothed green 2.5 pt, click dots 4 pt red; drawn up to playhead via 1/30 s periodic time observer; aspect-fit letterbox math shared in CursorOverlayView.fittedRect | High-contrast raw-vs-smoothed comparison is the whole point of the Phase 2 debug view |
| 2026-09-15 | Headless smoke path | Custom `main.swift`: `-open <path> -smokeTest` runs synchronous bundle load/path check and exits BEFORE AppKit boots; `NSQuitAlwaysKeepsWindows=false` registered in willFinishLaunching | SSH/headless sessions never render SwiftUI, and repeated `kill -9` during dev triggered the "restore windows?" modal that blocked didFinishLaunching |
| 2026-09-15 | Auto-zoom constants (§3.3) | T_merge 1.5 s; spatial radius 0.25×video width measured from the LAST activity (allows click-trail chains); anticipation 300 ms; zoom-out delay 1.0 s; default zoom 2.0 (min 1.5, max 1/0.35≈2.86 so ≥35% of width stays visible); full-screen when cluster bbox > 60% of an axis; pan when centers < 0.4×width apart and Δzoom < 0.5; weak activity needs ≥2 events or a click | Tuned to the fixture: produces 7 sensible keyframes on the 30 s sample; center-based radii made "spans the full screen" unreachable, hence last-activity chaining |
| 2026-09-15 | Camera transition easing | Critically-damped spring response s(t)=1−(1+ωt)e^(−ωt), normalized to hit exactly 0/1 at the gap ends; Focused ω=12, Smooth ω=5; zoom lerped in log space | Normalization guarantees no discontinuity entering holds; log-space zoom keeps multiplicative feel uniform |
| 2026-09-15 | Renderer: hybrid Metal+SwiftUI | Metal (runtime-compiled shader source, no .metal build step) renders video quad + camera transform + synthetic cursor + webcam layouts into an MTKView; SwiftUI owns background/padding/rounded corners/shadow OUTSIDE the MTKView | Master prompt allows hybrid; styling in SwiftUI is far simpler and the Metal pass stays a flat blit pipeline |
| 2026-09-15 | Frame pull | AVPlayerItemVideoOutput (32BGRA) + view.displayLink; pull on tick, setNeedsDisplay on paused MTKView | Standard 60 fps path; keeps up at 1080p without blocking AVPlayer |
| 2026-09-15 | Motion blur | Shutter seconds = strength/60; camera taps = 1+strength×15 accumulated in the fragment shader along camera velocity (uv space); cursor = ghosted trail draws (alpha ramps to an opaque head) along smoothed-path velocity; static cursor/camera collapses to 1 tap | Equal-alpha accumulation of a static cursor looked washed out (≈63% opacity); head+trail avoids that |
| 2026-09-15 | Synthetic cursor | Arrow polygon drawn into a 256×256 premultiplied BGRA texture; on-screen height = 4.5% of view height × cursorSize, NOT zoom-scaled | Vector-crisp at any zoom; constant output-space size matches Screen Studio's cursor-size setting semantics |
| 2026-09-15 | Webcam layouts | Bubble = square crop, circle mask in fragment shader (smoothstep edge), 22% of view width, bottom-right, 3.5% margin; webcam-full = aspect-fill crop, screen + cursor hidden; two AVPlayers seek/played together (unsynchronized, preview-grade) | Screen Studio style; exact A/V sync is an export concern (Phase 4), preview drift is acceptable |
| 2026-09-15 | Keyframe persistence | project.json v2 = v1 fields + optional keyframes/editorSettings (v1 files still decode); edits debounce-persist after 0.4 s | Every keyframe is a first-class editable object per §3.3.6 |
| 2026-09-15 | Timeline track | Custom SwiftUI lane: drag to move, 8 pt edge hot-zones to trim, Del/context-menu delete, double-click empty adds a 2 s manual zoom at playhead-free position | No external deps, matches "simple and robust" |
| 2026-09-15 | BUGFIX (partial): CVMetalTexture lifetime | `CVMetalTextureGetTexture()`'s MTLTexture is only valid while the `CVMetalTexture` wrapper is retained; the renderer now retains both per player. Real latent bug, but NOT sufficient — see next entry | Standalone pull script proved AVFoundation healthy (232 pulls, status 0) |
| 2026-09-15 | BUGFIX root cause: black Metal preview | `NSView.displayLink(target:selector:)` was called in `makeNSView` BEFORE the MTKView was in a window — the returned link is not registered with any run loop and never fires, so the pull loop was dead: zero pull attempts, `screenTexture` nil forever, black canvas. SwiftUI overlay + static Metal draws made the view look alive. Fix: `PreviewMTKView.viewDidMoveToWindow` installs the link once windowed AND explicitly `link.add(to: .main, forMode: .common)`. Verified by in-app stderr counters (0 pull attempts before; 120/120 after) and own screenshots: bars at 1x, 2x zoom hold, bubble + webcam-full all render | Found via live GUI loop (screencapture + clicks); supersedes the lifetime-only diagnosis above |
| 2026-09-15 | Renderer diagnostics | Unbuffered stderr logging (`FileHandle.standardError.write`): attach/first-pull/display-link-install one-liners, plus an UNHEALTHY counter dump only when pulls/draws stay zero | print() is block-buffered when stdout is redirected — invisible live; periodic spam gated off when healthy |
| 2026-09-15 | Single compositor for preview + export | `FrameComposer` (Metal, runtime-compiled shaders) renders the ENTIRE canvas — background gradient/wallpaper, drop shadow (SDF rounded rect), video quad with SDF rounded corners, webcam bubble/full, cursor. Preview encodes into an MTKView drawable; export into offscreen pixel-buffer textures. Frame state comes ONLY from `FrameStateBuilder.make`; project inputs ONLY from `ProjectAnalysis.load` | Acceptance gate is preview/export pixel-match: with one encode path and one state builder they cannot diverge. The Phase 3 SwiftUI hybrid styling was replaced by Metal SDF styling for exactly this reason |
| 2026-09-15 | Export encoder | H.264 High Auto via AVAssetWriter, bitrate = width×height×fps×0.12 (~15 Mbps at 1080p60), BGRA pixel-buffer pool with MetalCompatibility, PTS = frameIndex/fps, `await buffer.completed()` per frame | Simple serialized render loop is fast enough (30 s fixture exports in seconds) and deterministic |
| 2026-09-15 | Export frame sampling | Sequential AVAssetReader per video, nearest-behind frame at output time (0.5 ms epsilon); webcam time = screenTime − (webcamFirstHost − screenFirstHost), clamped ≥ 0 | recording-meta.json host-clock offsets make sync arithmetic exact; nearest-behind handles 60→30 fps without a resampler |
| 2026-09-15 | Export audio | recording.mov audio track passed through COMPRESSED (nil outputSettings + sourceFormatHint), skipped/shifted by trim start; webcam.mov audio NOT mixed | Mic-mute state is already baked into webcam.mov; full mic+system mixing is a Phase 5 concern |
| 2026-09-15 | Aspect re-targeting (§3.4.7) | Zoom semantics redefined as "fraction of frame HEIGHT visible" (aspect-independent): retargeted window = H/zoom tall × (H/zoom)·targetAspect wide, center preserved and clamped into source; 1x segments become centered best-fit reframe. Applied to keyframes BEFORE camera evaluation | Height-basis keeps zoom feel across aspects and never exceeds the source; width-basis forced zoom→1 for 9:16 which looked wrong |
| 2026-09-15 | Trim = In/Out range only | TrimRangeView handles on a bar above the zoom track, stored in EditorSettings.trimStart/trimEnd (optional → Phase 3 project.json still decodes); export clamps range and re-bases PTS at 0. Speed-up segments DEFERRED to Phase 5 | Master prompt allows minimal trim for Phase 4; speed affects readers, PTS math AND audio, so it gets its own phase |
| 2026-09-15 | Export CLI | `VibeStudio -open <bundle> -export <out.mp4> [-aspect] [-preset] [-range a,b]` runs synchronously pre-AppKit (semaphore + Task), prints progress/duration/size to stderr | Headless verification path; same ExportRenderer as the UI panel |
| 2026-09-15 | Click ripples | Ring quad at click position, 0.5 s lifetime: ease-out expansion 15%→95% of a 0.11-quad-height quad, quadratic fade from 0.85 alpha, plus a shrinking center dot; computed in FrameStateBuilder (zooms with camera), drawn by a dedicated ripple shader, clipped to the video quad | Matches Screen Studio's subtle click feedback; one code path covers preview + export |
| 2026-09-15 | Keystroke badges | Pill textures (dark 85% bg, white bold text, 64 px bitmap) cached per badge string; bottom-center row, 8.5% of quad height, up to 3 stacked; visibility 1.5 s (0.1 s fade-in, 0.3 s fade-out); modifiers in fixed ⌃⌥⇧⌘ order | AppKit NSString drawing lands inverted in the y-up bitmap AND needs a live NSGraphicsContext — final form: NSGraphicsContext wrapper + explicit row flip before texture upload (verified by screenshot) |
| 2026-09-15 | Hide static cursor | Threshold 4 px, dwell 1.0 s, fade 0.3 s, computed by walking the smoothed path back from t while samples stay within the threshold | Anchor-walk makes slow drift "moving" and sub-threshold jitter "static"; optional Bool in settings so old project.json decodes |
| 2026-09-15 | Loop cursor end | Synthetic 1.5 s post-segment (smoothstep): camera lerps to full frame (log-zoom), cursor lerps to its first position; export range extends by 1.5 s; preview slider extends and FrameStateBuilder maps t > duration into the segment | Seamless loops per spec §1.2; preview shows it only by dragging past the end (AVPlayer can't play past the media end) |
| 2026-09-15 | GIF export (§3.5) | Two-pass: ≤12 sampled frames → median-cut 256 palette → per-frame 3-3-2 RGB grid LUT (64-entry precompute) → indexed CGImage (CGColorSpace(indexedBaseSpace:)) → ImageIO; 480 px height cap, fps = min(30, source); loopCount 0 | ImageIO quantizes per-frame without a palette hook, so palette mapping is manual; LUT avoids 256-way nearest search per pixel |
| 2026-09-15 | Presets | Named EditorSettings JSON in ~/Library/Application Support/VibeStudio/Presets; apply menu, save field, copy-JSON-to-clipboard / import-from-clipboard sharing | Spec requires save/share with NO network — clipboard is the share channel |
| 2026-09-15 | Debug overlay & export | Raw/Smoothed path toggles are view-model-local SwiftUI state, never persisted, never passed to FrameStateBuilder/ExportRenderer | Exports cannot include the debug overlay by construction; verified visually in extracted frames |
| 2026-09-15 | Speed-up segments | DEFERRED (future work) | Phase 5 priority list 1–7 landed; speed regions affect readers, PTS math and audio and were explicitly skippable |
| 2026-09-15 | BUGFIX: mixed CGEvent timestamp domains | On macOS 26 (Mac15,14, mach timebase 125/3) MOST tap events carry mach ticks, but ~2.3% arrive with timestamps already in nanoseconds — converting those again inflated `t` ~41.7× into raw-host-looking values (bug evidence: 242/10459 events with t≈1.68M). Fix: `EventLogger.sanitizedHostSeconds` — accept the converted timestamp only within 5 s of `CACurrentMediaTime()` at receipt, else use receipt time | A valid tap timestamp is always within ms of receipt; clamping is airtight and domain-agnostic. Verified: zero out-of-range events in two live recordings |
| 2026-09-15 | BUGFIX: pill timer ran during pause | `SharedPauseClock.mediaTime` subtracted only COMPLETED pauses; during an ongoing pause it kept advancing. Added `PauseCompensator.mediaAdjusted` (freezes at pause start) used by mediaTime; writers keep `adjusted` (correct for buffers); events during pause stay dropped | Unit-tested (freeze during pause, resume math); verified live: pill showed frozen 0:12 during a 5.5 s pause, video duration = wall − pause ± 1 s |
| 2026-09-15 | Video-stall diagnostics (bug: 6 s stall, no moov) | Original failure did NOT reproduce in two live recordings (67 s + 26 s w/ pause; 3353 + ~1200 frames, 0 failed appends). Added permanent stderr diagnostics instead of guessing: per-queue recv/appended/dropped/failed counters every 5 s, first-append-failure log with writer error, SCStreamDelegate didStopWithError log, finish() status logging (teardown no longer swallows finish errors with `try?`) | If the stall recurs, the log will say whether frames stopped arriving or appends started failing; previously the failure was invisible and moov loss was silent |
| 2026-09-15 | Live machine capture facts | Main display 3440×1440 @ scale 1, SCStream delivers 50 fps (50 Hz refresh mode), mach timebase 125/3 | Frame-rate-agnostic code paths handled it; worth knowing when interpreting ffprobe output |
| 2026-09-15 | Preview-vs-export consistency tolerance | Automated test renders t=2.1/10.5/25.0 through the REAL FrameStateBuilder+FrameComposer (preview path) and the REAL ExportRenderer (H.264 → AVAssetReader decode). Tolerances: mean abs luma diff < 3.0/255 and PSNR > 40 dB. Measured: diff 0.32–0.41, PSNR 49.6–51.3 dB | Pixel-identical is impossible by definition (H.264 quantization); luma-only comparison because chroma subsampling is encoder-owned. Thresholds sit ~7x above measured encoder noise, so the test fails on structural divergence (wrong camera, missing cursor/ripple/badge, layout mismatch) but never on codec jitter |
| 2026-09-15 | Synthetic input lesson | CGEventPost mouse clicks do NOT reach the pill NSPanel; keyboard (⌘⇧2 hotkey post) works, and Accessibility clicks via `osascript -e 'tell application "System Events" to tell process "VibeStudio" to click button N of group 1 of window 1'` work (buttons expose help texts) | Saved from the on-machine verification session; recorded in AGENTS.md for future agents |
| 2026-09-15 | AVKit linkage | `_forceAVKitLinkage = AVPlayerView.self` in EditorView | SwiftUI VideoPlayer crashed at launch ("failed to demangle superclass of VideoPlayerView 'So12AVPlayerViewC'") because AVPlayerView's ObjC superclass was dead-stripped; direct symbol ref forces linking |
| 2026-09-15 | Dev saved-state hygiene | delete dev.vibestudio.app.savedState after crashes | macOS crash-recovery modal blocks headless launch verification |
